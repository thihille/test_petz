import styled from 'styled-components';

export const Banner = styled.div`
  position: relative;
  overflow: hidden;
  width: 100%;

  div.anin {
    position: absolute;
    left: 0;
    right: 0;
    margin: 0 auto 0 180px;
  }
  img {
    width:100%;
  }
`;
